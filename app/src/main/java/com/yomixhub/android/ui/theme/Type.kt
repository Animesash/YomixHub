package com.yomixhub.android.ui.theme

import androidx.compose.material3.Typography

/**
 * Material 3 type scale on top of the platform Roboto.
 *
 * Roboto is the system default for [Typography], so no FontFamily overrides are
 * needed. Letter-spacing and weights follow the M3 spec:
 *
 *  display 57/64   headline 28/36   title 16/24   body 14/20   label 11/16
 */
val YomixTypography = Typography()
